# Review Bundle — TASK-007 — Public README and portfolio packaging

## Files changed

```
$ git diff --stat main
 README.md             | 213 ++++++++++++++++++++++++++++++++------------------
 docs/CURRENT_STATE.md |  31 +++++++-

$ git status --porcelain --untracked-files=all
 M README.md
 M docs/CURRENT_STATE.md
?? docs/tickets/TASK-007-public-readme-and-portfolio-packaging.md
?? examples/sample_output.json
?? examples/sample_quote.txt
```

Only files in the ticket's file scope were touched. No backend/frontend source
files, `package.json`/`package-lock.json`, `backend/.env`, or `logs/` changed.

## Acceptance criteria — evidence

**1. README opens with product explanation, not setup commands.**

`README.md` now opens with the title, a one-line positioning statement, the v0/
disclaimer callout, and a "What it is, who it helps, why it exists" section (who:
"someone about to approve a confusing service/repair/parts quote"; why:
"understand-before-approve, not just audit") before any setup or architecture
content. The former opening — a "deliberately built like a deployable LLM product"
tooling bullet list — was moved down into the new "Why this is portfolio-credible"
section near the end, reworded as factual statements rather than a leading pitch.

**2. Demo-mode no-key path stated plainly near the top, copy-pasteable.**

The "Try it in under a minute (no API key needed)" section (immediately after the
product framing) states: "no `backend/.env` file and no OpenAI API key required."
Backend/frontend commands are unchanged, verified copy-pasteable code blocks (same
commands previously verified working in TASK-006).

**3. Setup explicitly requires an activated Python environment; no committed
environment file stated honestly.**

Inspected the repo for environment/lockfile support before writing this:

```
$ cat backend/requirements.txt
fastapi==0.128.6
uvicorn==0.40.0
pydantic==2.12.5
openai==2.24.0
python-dotenv==1.2.1

$ cat environment.yml
cat: environment.yml: No such file or directory

$ cat pyproject.toml
cat: pyproject.toml: No such file or directory

$ conda env list
...
quotecheck               /home/akshay/miniconda3/envs/quotecheck
...

$ conda run -n quotecheck python -c "import fastapi, uvicorn, pydantic, openai, dotenv; print('deps ok')"
deps ok
```

Only `backend/requirements.txt` is committed; there is a local `quotecheck` conda
env with the deps installed, but nothing in the repo enforces or reproduces that.
README's "Prereqs" now reads: "Python 3.10+ **in an activated environment** (conda
recommended below — there is no committed `environment.yml`/lockfile yet, only a
pinned `backend/requirements.txt`; see Limitations)." The backend setup steps show
`conda create` → `conda activate` → `pip install -r backend/requirements.txt` →
`uvicorn ...`, in that order, matching the real supported path. The same point is
repeated in the Limitations section as a stated gap, not glossed over, and added to
`docs/CURRENT_STATE.md`'s Gaps list. No `environment.yml`/lockfile was created —
per the correction, this ticket is packaging, not dependency restructuring.

**4. OpenAI mode marked optional, same config steps as before.**

"Demo mode vs. OpenAI mode" section: unchanged `cp backend/.env.example
backend/.env` / `QUOTECHECK_USE_OPENAI=1` / `OPENAI_API_KEY` / `QUOTECHECK_MODEL`
steps, carried over verbatim from the prior README (previously verified in
TASK-006).

**5. Real sample report present (excerpt + full files).**

Generated by actually running the backend and calling `/analyze` (not hand-written):

```
$ conda run -n quotecheck uvicorn backend.app:app --host 127.0.0.1 --port 8000 &
$ curl -s http://127.0.0.1:8000/health
{"status":"ok"}

$ mkdir -p examples
$ curl -s -X POST http://127.0.0.1:8000/analyze -H "Content-Type: application/json" \
  -d '{"quote_text":"Brake pads replacement recommended. Tyre rotation. Shop supplies / misc service charge included."}' \
  | python3 -m json.tool | tee examples/sample_output.json
{
    "line_items": [
        { "name_raw": "Brake service/ pads (from quote)", "risk_level": "red", "vague_or_confusing": false, ... },
        { "name_raw": "Tyre replacement (from quote)", "risk_level": "yellow", "vague_or_confusing": false, ... },
        { "name_raw": "Other/unspecified charges (from quote)", "risk_level": "yellow", "vague_or_confusing": true, ... }
    ],
    "overall_summary": [...],
    "verification_questions": [...],
    "things_to_verify": [...],
    "uncertainty_markers": {...},
    "refusals": [],
    "disclaimer": "QuoteCheck is a v0 prototype; ...",
    "metadata": {
        "prompt_version": "quotecheck_v0.2",
        "model": "quotecheck-demo-analyzer",
        "created_at": "2026-07-08T10:52:45.362892Z",
        "request_id": "c1a631de-43ce-48b4-8c55-f05affce456b",
        "latency_ms": 0,
        "schema_valid": true
    }
}
```

`examples/sample_quote.txt` was written with the exact text of the frontend's
built-in sample quote (`frontend/src/App.jsx:66-67`) so it stays in sync with what a
visitor sees in the app. `README.md`'s "What a report looks like" section shows this
input, an inline excerpt of the first line item, a note about the vague/confusing
third item, and links to both `examples/` files, with a note that
`request_id`/`created_at`/`latency_ms` will differ per run.

**6. `examples/sample_output.json` model label + provenance.**

```
$ python3 -c "import json;d=json.load(open('examples/sample_output.json'));print(d['metadata']['model'])"
quotecheck-demo-analyzer
```

Confirmed Demo mode (`QUOTECHECK_USE_OPENAI=0`, the default — no `backend/.env` was
created or used in this session) actually served the request; the corresponding
JSONL log entry for the same `request_id` confirms it end-to-end:

```
$ tail -n 1 logs/app_runs.jsonl | python3 -m json.tool
{
    "event": "quotecheck_analyze",
    "created_at": "2026-07-08T10:52:45.362953Z",
    "request_id": "c1a631de-43ce-48b4-8c55-f05affce456b",
    "prompt_version": "quotecheck_v0.2",
    "model": "quotecheck-demo-analyzer",
    "latency_ms": 0,
    "schema_valid": true,
    "num_items": 3,
    "risk_counts": {"red": 1, "yellow": 2, "green": 0},
    "uncertainty": {"ambiguous_items_present": true, "missing_vehicle_context": true, "needs_mechanic_confirmation": true},
    "error": null
}
```

Backend was stopped after capture:

```
$ pkill -f "uvicorn backend.app:app"
$ curl -s -m 2 http://127.0.0.1:8000/health; echo "exit:$?"
exit:7
```

**7. Architecture explained simply, existing diagram kept.**

The README's "Architecture" section keeps the same ASCII diagram
(Browser → POST /analyze → FastAPI Backend → logs/app_runs.jsonl) from the prior
version, trimmed to sit under one heading with the observability/prompt-versioning
prose folded in underneath instead of as separate top-level sections — no content
removed, just consolidated for scannability.

**8. Limitations section honest, matches `docs/CURRENT_STATE.md` / `SPEC.md`.**

New "Limitations" section explicitly states: no production-readiness claims (no
auth/DB/SLAs/hardening), paste-text only (no PDF/OCR), price benchmarking **not
implemented**, no committed environment/lockfile (cross-checked against item 3
above), no repair/retry yet, no eval harness/test suite, vehicle-service-flavored
taxonomy (narrower than `SPEC.md`'s general target), and that QuoteCheck doesn't
verify vendor claims or replace a professional — each of these matches an existing
line in `docs/CURRENT_STATE.md`'s Gaps section or `SPEC.md`'s Non-goals section; no
new claims were invented.

**9. "Why this is portfolio-credible" section, non-inflated.**

Added as its own section, five bullet points, each naming a concrete, already-true
mechanism (Pydantic schema contract, `DEMO_ANALYZER_MODEL` honest labeling, JSONL
observability, the `docs/tickets/`+`docs/review/` workflow, stated limitations) — no
metrics, no unverifiable claims, no adjectives beyond describing what exists.

**10. No product behavior changes; diff scope confined.**

```
$ git diff --stat main
 README.md             | 213 ++++++++++++++++++++++++++++++++------------------
 docs/CURRENT_STATE.md |  31 +++++++-
 2 files changed, 169 insertions(+), 75 deletions(-)
```

Plus two new untracked files (`docs/tickets/TASK-007-...md`,
`docs/review/REVIEW_BUNDLE__TASK-007-...md`) and the new `examples/` directory. No
`backend/` or `frontend/` files appear in the diff or untracked list.

**11. `docs/CURRENT_STATE.md` updated.**

"Last updated" line changed to `2026-07-08 (TASK-007)`; a "Fixed in TASK-007"
section was added (above "Fixed in TASK-006") documenting the README rewrite, the
new `examples/` files and their provenance, and the no-behavior-change /
no-new-dependency /no-ARCHITECTURE.md notes; the Gaps section gained a bullet about
the missing `environment.yml`/lockfile.

**12. No secrets committed.**

```
$ grep -RInE '\bsk-[A-Za-z0-9_-]{10,}|api_key\s*=\s*["'"'"'][^"'"'"']{10,}' README.md examples/ docs/CURRENT_STATE.md docs/tickets/TASK-007-public-readme-and-portfolio-packaging.md
(no output, grep exit 1 — no matches)
```

## Commands run (full list)

```bash
cat backend/requirements.txt
cat environment.yml 2>&1 || true
cat pyproject.toml 2>&1 || true
conda env list
conda run -n quotecheck python --version
conda run -n quotecheck python -c "import fastapi, uvicorn, pydantic, openai, dotenv; print('deps ok')"
conda run -n quotecheck uvicorn backend.app:app --host 127.0.0.1 --port 8000 &
curl -s http://127.0.0.1:8000/health
mkdir -p examples
curl -s -X POST http://127.0.0.1:8000/analyze -H "Content-Type: application/json" \
  -d '{"quote_text":"Brake pads replacement recommended. Tyre rotation. Shop supplies / misc service charge included."}' \
  | python3 -m json.tool | tee examples/sample_output.json
tail -n 1 logs/app_runs.jsonl | python3 -m json.tool
pkill -f "uvicorn backend.app:app"
curl -s -m 2 http://127.0.0.1:8000/health; echo "exit:$?"
python3 -c "import json;d=json.load(open('examples/sample_output.json'));print(d['metadata']['model'])"
git status --short
git diff --stat main
git status --porcelain --untracked-files=all
grep -RInE '\bsk-[A-Za-z0-9_-]{10,}|api_key\s*=\s*["'"'"'][^"'"'"']{10,}' README.md examples/ docs/CURRENT_STATE.md docs/tickets/TASK-007-public-readme-and-portfolio-packaging.md
```

## Out-of-scope notes

- No screenshot was added: no Playwright/Puppeteer/Chromium is installed
  (`frontend/node_modules/.bin` has no browser-automation binaries, `npm ls
  playwright puppeteer` returns empty), and installing one would be a new
  dependency — explicitly out of scope per the ticket. Per user decision, the
  README instead has a placeholder "Screenshot" section describing how to add one
  later.
- No `environment.yml`/`pyproject.toml`/lockfile was created. Per the user's
  correction, this ticket documents the real, currently-supported setup path (conda
  env + `backend/requirements.txt`) honestly, including the reproducibility gap, but
  does not restructure dependency management — that's out of scope for a packaging
  ticket.
- No `docs/ARCHITECTURE.md` was added — the existing inline README diagram was
  judged sufficient for this repo's size; a separate file would duplicate content.
- `npm run lint`/`npm run build` were not re-run since no frontend files were
  touched by this ticket (verified via `git status --porcelain` above showing no
  `frontend/` entries).

## Definition of done

- [x] Implementation complete within file scope.
- [x] `examples/sample_quote.txt` and `examples/sample_output.json` committed
      (pending user commit), generated from a real Demo-mode `/analyze` call.
- [x] `docs/CURRENT_STATE.md` "Last updated" line and "Fixed in TASK-007" entry
      added.
- [x] Ticket file (`docs/tickets/TASK-007-public-readme-and-portfolio-packaging.md`)
      written.
- [x] This review bundle written with concrete evidence, no placeholders.
- [x] No secrets committed; no backend/frontend files changed.
- [x] Work left uncommitted on `task/007-public-readme-and-portfolio-packaging` —
      the user will review and commit manually.
